// Secrets.xcconfig.template — version controlled.
// Copy this file to Secrets.xcconfig and fill in your values.

// Note: // is the xcconfig comment delimiter, so the Worker URL must be split.
LETTERLY_WORKER_SCHEME = https
LETTERLY_WORKER_HOST = letterly-worker.<your-subdomain>.workers.dev
